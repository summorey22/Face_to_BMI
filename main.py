import bmi_model, bmi_test, convert_model

#Copy photos and dataset.csv in this folder and replace the variables 'photos_path' and 'csv_path' in bmi_model.py with the path of those two respectively

#Initially, remove the comment from the following line and run it
#bmi_model.train_model()

#After that, run the following line of code to convert the .pb to .tflite
#convert_model.tf_model()

#With the model.tflite ready, any .jpg can be used to test the model. Save the image file in the root directory and replace the path arguement with the image path in the function given below
#bmi_test.test_image(path)



